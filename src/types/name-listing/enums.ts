export enum DeadlineType {
    CANT_MINT_AFTER_EXPIRY = 1
}

export enum ListingCurrencyType {
    ETH = "ETH",
    USD = "DOLLAR"
}

export enum ListingVersionType {
    L1 = 'L1',
    L2 = 'L2',
}

export enum ListingTokenType {
    ERC20 = "ERC20",
    ERC721 = "ERC721",
    ERC1155 = "ERC1155"
}


export enum WhitelistType {
    ONLY_WHITELISTED = 1,
    FREE_MINT = 2,
}